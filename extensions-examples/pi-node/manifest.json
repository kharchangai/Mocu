{
  "id": "pi-node",
  "name": "Pi Agent",
  "description": "Embeds a persistent pi coding-agent session with live Mocu chat controls for steering, pausing and resuming tasks.",
  "version": "0.1.0",
  "runtime": "node",
  "entry": "index.js",
  "engines": {
    "node": ">=20"
  },
  "permissions": [
    "filesystem",
    "shell"
  ],
  "commands": [
    {
      "id": "ask",
      "title": "Ask pi",
      "description": "Run pi with live progress; steer it, stop and resume the same task, or cancel from Mocu chat.",
      "streaming": true,
      "interactive": true,
      "timeoutSeconds": 0
    },
    {
      "id": "reset",
      "title": "Reset pi session",
      "description": "Dispose the current pi session so the next 'ask' starts fresh."
    },
    {
      "id": "status",
      "title": "Pi session status",
      "description": "Shows whether a pi session is active, its cwd, model and thinking level."
    }
  ]
}
